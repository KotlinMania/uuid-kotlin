# Rust callers — `uuid-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `uuid` | `uuid` | 63 | 17 | 4 | 58 |

## `uuid`

### Inbound symbols (2 distinct, 63 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `uuid`.

#### `uuid::Uuid` — 61 site(s) (58 codex, 3 other, 16 caller crates)

- ✅ `codex-kotlin/app_test_support` `rollout.rs:16` — `use uuid::Uuid`
- ✅ `codex-kotlin/codex-app-server` `src/filters.rs:89` — `use uuid::Uuid`
- ✅ `codex-kotlin/codex-app-server` `src/outgoing_message.rs:674` — `use uuid::Uuid`
- ✅ `codex-kotlin/codex-app-server` `src/request_processors.rs:413` — `use uuid::Uuid`
- ✅ `codex-kotlin/codex-app-server` `tests/common/rollout.rs:16` — `use uuid::Uuid`
- ✅ `codex-kotlin/codex-app-server` `tests/suite/v2/memory_reset.rs:18` — `use uuid::Uuid`
- ✅ `codex-kotlin/codex-app-server` `tests/suite/v2/remote_thread_store.rs:49` — `use uuid::Uuid`
- ✅ `codex-kotlin/codex-app-server` `tests/suite/v2/thread_list.rs:45` — `use uuid::Uuid`
- ✅ `codex-kotlin/codex-app-server` `tests/suite/v2/thread_read.rs:72` — `use uuid::Uuid`
- ✅ `codex-kotlin/codex-app-server` `tests/suite/v2/thread_resume.rs:80` — `use uuid::Uuid`
- ✅ `codex-kotlin/codex-app-server-protocol` `src/export.rs:2097` — `use uuid::Uuid`
- ✅ `codex-kotlin/codex-app-server-protocol` `src/protocol/thread_history.rs:58` — `use uuid::Uuid`
- _… and 49 more site(s)._

#### `uuid1::Uuid` — 2 site(s) (0 codex, 2 other, 1 caller crates)

- `schemars-kotlin/schemars` `src/json_schema_impls/uuid1.rs:4` — `use uuid1::Uuid`
- `schemars-kotlin/schemars` `tests/integration/uuid.rs:2` — `use uuid1::Uuid`

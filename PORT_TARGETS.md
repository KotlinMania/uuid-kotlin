# Port targets — `uuid-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `uuid` that codex-rs uses, and therefore the minimum subset that `uuid-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-181] (rank `21`, tier `direct`, status `To Do`)

**Cargo package name**: `uuid`
**Rust module name (in `use` statements)**: `uuid`

## Summary

- **1** distinct symbols imported by codex-rs
- **58** total `use` statements
- **54** codex source files affected
- **14** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `uuid::Uuid`

- **58 `use` site(s)** across **14 codex member crate(s)**: `app_test_support`, `codex-app-server`, `codex-app-server-protocol`, `codex-app-server-test-client`, `codex-core`, …

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-protocol/src/export.rs:2097` (`codex-app-server-protocol`) — `use uuid::Uuid`
- `app-server-protocol/src/protocol/thread_history.rs:58` (`codex-app-server-protocol`) — `use uuid::Uuid`
- `app-server-protocol/src/protocol/thread_history.rs:1231` (`codex-app-server-protocol`) — `use uuid::Uuid`
- `app-server-test-client/src/lib.rs:87` (`codex-app-server-test-client`) — `use uuid::Uuid`
- `app-server/src/filters.rs:89` (`codex-app-server`) — `use uuid::Uuid`
- `app-server/src/outgoing_message.rs:674` (`codex-app-server`) — `use uuid::Uuid`
- `app-server/src/request_processors.rs:413` (`codex-app-server`) — `use uuid::Uuid`
- `app-server/tests/common/rollout.rs:16` (`codex-app-server`) — `use uuid::Uuid`
- `app-server/tests/common/rollout.rs:16` (`app_test_support`) — `use uuid::Uuid`
- `app-server/tests/suite/v2/memory_reset.rs:18` (`codex-app-server`) — `use uuid::Uuid`
- `app-server/tests/suite/v2/remote_thread_store.rs:49` (`codex-app-server`) — `use uuid::Uuid`
- `app-server/tests/suite/v2/thread_list.rs:45` (`codex-app-server`) — `use uuid::Uuid`
- `app-server/tests/suite/v2/thread_read.rs:72` (`codex-app-server`) — `use uuid::Uuid`
- `app-server/tests/suite/v2/thread_resume.rs:80` (`codex-app-server`) — `use uuid::Uuid`
- `core/src/event_mapping.rs:21` (`codex-core`) — `use uuid::Uuid`
- `core/src/installation_id.rs:15` (`codex-core`) — `use uuid::Uuid`
- `core/src/installation_id.rs:73` (`codex-core`) — `use uuid::Uuid`
- `core/src/session/mod.rs:164` (`codex-core`) — `use uuid::Uuid`
- `core/src/tasks/user_shell.rs:13` (`codex-core`) — `use uuid::Uuid`
- `core/src/tools/handlers/agent_jobs.rs:36` (`codex-core`) — `use uuid::Uuid`
- _… and 38 more (see `codex_external_usage.json` for the full list)._
